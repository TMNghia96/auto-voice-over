import fs from 'fs';
import { parseSrt } from './src/lib/SrtOptimizer';
import { generateAllAudio } from './src/services/PiperService';

async function test() {
    // Read an SRT file. I'll read any available SRT in the workspace.
    // wait, where's the SRT? The user said "các câu có từ đơn số 57, ..."
}
